plugins {
    id 'java'
    id 'application'
}

defaultTasks 'shadowJar'

jar {
    mainClassName = 'gerefi.GccMapReader'

    destinationDirectory = file( '$rootDir/../..' )
    manifest {
        attributes(
            'Main-Class': 'gerefi.GccMapReader'
        )
    }
}